---
title: "Authentication"
description: "Authenticate with API keys."
---

## API keys

Create keys in Settings -> API. Keys are scoped to an organization.

## Example

```bash
curl -H "Authorization: Bearer YOUR_API_KEY" \
  https://api.factoryflow.com/v1/work-orders
```
