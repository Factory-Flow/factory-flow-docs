---
title: "API introduction"
description: "Build custom automation with Factory Flow."
---

## Base URL

Use the base URL for your environment.

## Authentication

API keys are scoped to an organization and role.

## Ingestion

Machine ingestion endpoints validate payloads and enforce per-machine rate limits.
