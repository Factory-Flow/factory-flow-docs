---
title: "Introduction"
description: "Interactive API reference for the Factory Flow REST API."
---

Use the API playground to explore Factory Flow endpoints and run requests against the production API.

<div style={{ display: "flex", gap: "12px", margin: "16px 0" }}>
  <a href="/api-reference/openapi.json" target="_blank" style={{ textDecoration: "none" }}>
    <button style={{ padding: "8px 16px", borderRadius: "6px", border: "1px solid #ccc", cursor: "pointer", background: "transparent" }}>
      View OpenAPI spec
    </button>
  </a>
  <a href="/api-reference/openapi.json" download="openapi.json" style={{ textDecoration: "none" }}>
    <button style={{ padding: "8px 16px", borderRadius: "6px", border: "1px solid #ccc", cursor: "pointer", background: "transparent" }}>
      Download OpenAPI spec
    </button>
  </a>
</div>

## Authentication

All endpoints use Bearer token authentication. Generate a token in **Organization Settings → API Tokens**, then use it in the playground.

```json
"security": [
  {
    "bearerAuth": []
  }
]
```
