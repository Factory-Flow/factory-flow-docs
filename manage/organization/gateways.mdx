---
title: "Gateways"
icon: "router"
description: "Register and manage Factory Flow Gateway devices that collect data from your machines."
---

Gateways are the IoT devices or edge computers that collect data from your machines and forward it to Factory Flow. Each gateway needs to be registered here before it can send data.

To register a gateway, click **New Gateway**, give it a name and description, and save. You'll receive a unique identifier to configure on the gateway device itself.

<Frame>
  <img className="block dark:hidden" src="/images/manage/org-gateways.png" alt="Gateways list showing registered gateway devices with their unique identifiers and status" />
  <img className="hidden dark:block" src="/images/manage/org-gateways-dark.png" onError={(e) => { e.currentTarget.src = "/images/manage/org-gateways.png"; }} alt="Gateways list showing registered gateway devices with their unique identifiers and status" />
</Frame>
